group 'com.jmp2017w.apache'
version '1.0-SNAPSHOT'

task zipApache(type: Zip) {
    from "${project.projectDir}/"
    include "*"
    include "*/*"
    exclude "build.gradle"
    archiveName "apache.zip"
    destinationDir(file("${project.projectDir}/../build"))
}